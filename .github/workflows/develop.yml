name: CI/CD - Develop
on:
  push:
    branches: [develop]
jobs:
  test:
    runs-on: macos-13
    name: Testing
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Test code
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 29
          script: ./gradlew test
